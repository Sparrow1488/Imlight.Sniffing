<UserControl x:Class="Imlight.Client.Desktop.Views.SnifferView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top">
            <TextBlock FontSize="28">Sniffer</TextBlock>
        </StackPanel>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MaxHeight="350">
            <TextBox MaxHeight="250" IsReadOnly="True" Text="{Binding ReadPacketMessage}"/>
            
            <WrapPanel>
                <Button Content="Start" Width="60" Height="20" Command="{Binding StartSniffingCommand}"></Button>
                <Button Content="Stop" Width="60" Height="20" Command="{Binding StopSniffingCommand}"></Button>
            </WrapPanel>
        </StackPanel>
    </Grid>
</UserControl>
